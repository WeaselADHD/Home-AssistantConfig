---
# Proxmox VE
proxmoxve:
  - host: !secret pve_host
    username: !secret pve_username
    password: !secret pve_password
    realm: pve
    verify_ssl: false
    nodes:
      - node: pve
        vms:
          - 100
          - 200
          - 201
          - 300
          - 400
